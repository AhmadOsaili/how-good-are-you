DROP POLICY "Partner members can update assigned leads" ON public.leads;
DROP POLICY "Partner members can view assigned leads" ON public.leads;
DROP POLICY "Partner members can view own assignments" ON public.lead_assignments;